{
  "name": "handle-slack-comm",
  "version": "1.0.0",
  "description": "handle comms with chat clients",
  "main": "app.js",
  "repository": "https://github.com/aws-samples/ops-health-ai",
  "author": "seanxw",
  "license": "MIT",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "esbuild": "^0.14.14",
    "@slack/web-api": "^7.2.0"
  },
  "devDependencies": {
  }
}
